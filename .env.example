# Copie este arquivo para ".env" (sem o ".example") e preencha com sua
# chave real. O arquivo ".env" nunca é enviado ao Git (está no
# .gitignore) — só existe na sua máquina.

ANTHROPIC_API_KEY=sk-ant-...
